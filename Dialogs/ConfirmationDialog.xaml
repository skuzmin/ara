<Window x:Class="ARA.Dialogs.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ARA.Dialogs" 
		mc:Ignorable="d"
		xmlns:controls="clr-namespace:ARA.Controls.CustomControls"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Title="ConfirmationDialog">
	<Border Style="{StaticResource Dialog__MainContainer}">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="32" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<TextBlock Grid.Row="0" Style="{StaticResource Dialog__Message}" x:Name="MessageEl" Text="Message" />
			<Grid Grid.Row="2">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>
				<controls:AraButton Grid.Column="0" Style="{StaticResource SecondaryButton}" IsCancel="True">
					<controls:AraButton.Children>
						<TextBlock x:Name="CancelTextEl" Text="Cancel"/>
					</controls:AraButton.Children>
				</controls:AraButton>
				<controls:AraButton Grid.Column="2" Style="{StaticResource PrimaryButton}" IsDefault="True" Click="ConfirmButton_Click">
					<controls:AraButton.Children>
						<TextBlock x:Name="ConfirmTextEl" Text="Delete"/>
					</controls:AraButton.Children>
				</controls:AraButton>
			</Grid>
		</Grid>
	</Border>
</Window>
