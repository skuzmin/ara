<local:AraWindow x:Class="ARA.Dialogs.ConfirmationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ARA"
		mc:Ignorable="d"
		xmlns:controls="clr-namespace:ARA.Controls.CustomControls"
		WindowStartupLocation="CenterOwner"
		Style="{StaticResource Dialog_Window}"
        Title="Confirmation Dialog">
	<Border Style="{StaticResource Dialog__MainContainer}">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="16" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<StackPanel Grid.Row="0" Orientation="Vertical">
				<TextBlock Style="{StaticResource Dialog__Message}" x:Name="MainMessageEl" Text="MainMessage" />
				<TextBlock Style="{StaticResource Dialog__Message}" x:Name="SubMessageEl" Text="SubMessage" />
			</StackPanel>

			<Grid Grid.Row="2">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>
				<controls:AraButton Grid.Column="0" IsCancel="True">
					<controls:AraButton.Style>
						<Style TargetType="controls:AraButton" BasedOn="{StaticResource SecondarySmallButton}">
							<Style.Triggers>
								<DataTrigger Binding="{Binding Text, ElementName=CancelTextEl}" Value="">
									<Setter Property="Visibility" Value="Collapsed" />
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</controls:AraButton.Style>
					<controls:AraButton.Children>
						<TextBlock x:Name="CancelTextEl" />
					</controls:AraButton.Children>
				</controls:AraButton>
				<controls:AraButton Grid.Column="2" IsDefault="True" Click="ConfirmButton_Click">
					<controls:AraButton.Style>
						<Style TargetType="controls:AraButton" BasedOn="{StaticResource PrimarySmallButton}">
							<Style.Triggers>
								<DataTrigger Binding="{Binding Text, ElementName=ConfirmTextEl}" Value="">
									<Setter Property="Visibility" Value="Collapsed" />
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</controls:AraButton.Style>
					<controls:AraButton.Children>
						<TextBlock x:Name="ConfirmTextEl" />
					</controls:AraButton.Children>
				</controls:AraButton>
			</Grid>
		</Grid>
	</Border>
</local:AraWindow>
