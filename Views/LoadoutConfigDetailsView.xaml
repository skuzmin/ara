<UserControl x:Class="ARA.Views.LoadoutConfigDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 mc:Ignorable="d"
			 xmlns:pages="clr-namespace:ARA.ViewModels.Pages" 
			 xmlns:controls="clr-namespace:ARA.Controls.CustomControls"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" d:DataContext="{d:DesignInstance Type=pages:LoadoutConfigDetailsViewModel}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="40" />
			<RowDefinition Height="12" />
			<RowDefinition Height="*" />
			<RowDefinition Height="12" />
			<RowDefinition Height="40" />
		</Grid.RowDefinitions>

		<Grid Grid.Row="0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="12" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>

			<controls:AraButton Grid.Column="0" Style="{StaticResource PageTitleButton}" Command="{Binding BackCommand}">
				<controls:AraButton.Children>
					<Image Style="{StaticResource ButtonImage}" Source="/Assets/Icons/chevron-left.png" />
				</controls:AraButton.Children>
			</controls:AraButton>
			<TextBlock Grid.Column="2" Style="{StaticResource PageTitle}" Text="{Binding Title}" />
		</Grid>

		<ScrollViewer Focusable="False" Grid.Row="2" VerticalScrollBarVisibility="Auto">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="16" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

				<!-- Name -->
				<StackPanel Grid.Row="0">
					<Label Style="{StaticResource AraLabel}" Target="{Binding ElementName=NameTextBox}" Content="{DynamicResource LoadoutConfigDetails.Name}"/>
					<controls:AraTextBox x:Name="NameTextBox" 
						Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
						PlaceholderText="{DynamicResource LoadoutConfigDetails.Name.Placeholder}" 
						Style="{StaticResource AraTextBox}"
						IsError="{Binding LoadoutValidation.IsNameNotValid}"
						MaxLength="50"/>
				</StackPanel>

				<!-- Items -->
				<GroupBox Grid.Row="2" Header="{DynamicResource LoadoutConfigDetails.Items}" Style="{StaticResource AraGroupBox}">
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="12" />
							<RowDefinition Height="*" />
						</Grid.RowDefinitions>

						<Grid Grid.Row="0">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*"/>
								<ColumnDefinition Width="12"/>
								<ColumnDefinition Width="Auto"/>
							</Grid.ColumnDefinitions>

							<controls:AraComboBox
								x:Name="ItemsComboBox"
								IsEditable="True"
								StaysOpenOnEdit="True"
								IsTextSearchEnabled="False"
								Style="{StaticResource AraComboBox}"
								PlaceholderText="{DynamicResource LoadoutConfigDetails.Items.Placeholder}"
								TextSearch.TextPath="Name"
								ItemsSource="{Binding ItemsList}"
								SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
								<controls:AraComboBox.ItemTemplate>
									<DataTemplate>
										<StackPanel Orientation="Horizontal">
											<Image Source="{Binding Path}" Height="32" Stretch="Uniform" VerticalAlignment="Center"/>
											<TextBlock Text="{Binding Name}" VerticalAlignment="Center" Margin="8 0 0 0"/>
										</StackPanel>
									</DataTemplate>
								</controls:AraComboBox.ItemTemplate>
							</controls:AraComboBox>

							<controls:AraButton Grid.Column="2" Command="{Binding AddItemCommand}">
								<controls:AraButton.Children>
									<Image Style="{StaticResource ButtonImage}" Source="/Assets/Icons/plus.png" />
									<TextBlock Text="{DynamicResource LoadoutConfigDetails.Items.Button}" Margin="4 0 0 0"/>
								</controls:AraButton.Children>
								<controls:AraButton.Style>
									<Style TargetType="controls:AraButton" BasedOn="{StaticResource SecondaryButton}">
										<Style.Triggers>
											<DataTrigger Binding="{Binding SelectedItem}" Value="{x:Null}">
												<Setter Property="IsEnabled" Value="False" />
											</DataTrigger>
										</Style.Triggers>
									</Style>
								</controls:AraButton.Style>
							</controls:AraButton>
						</Grid>

						<Grid Grid.Row="2">
							<!-- Loadout Items Grid -->
							<Border>
								<Border.Style>
									<Style TargetType="Border" BasedOn="{StaticResource DataGridBorderWrapper}">
										<Style.Triggers>
											<DataTrigger Binding="{Binding SelectedItemsList.Count}" Value="0">
												<Setter Property="Visibility" Value="Hidden"/>
											</DataTrigger>
										</Style.Triggers>
									</Style>
								</Border.Style>
								<DataGrid x:Name="LoadoutPreview" ItemsSource="{Binding SelectedItemsList}">
									<DataGrid.Columns>
										<!-- Icon -->
										<DataGridTemplateColumn Header="{DynamicResource LoadoutConfigDetails.Items.Grid.Icon}" Width="60">
											<DataGridTemplateColumn.CellTemplate>
												<DataTemplate>
													<Image Source="{Binding Path}" Height="48" Stretch="Uniform" VerticalAlignment="Center"/>
												</DataTemplate>
											</DataGridTemplateColumn.CellTemplate>
										</DataGridTemplateColumn>
										<!-- Name -->
										<DataGridTemplateColumn Header="{DynamicResource LoadoutConfigDetails.Items.Grid.Name}" Width="*">
											<DataGridTemplateColumn.CellTemplate>
												<DataTemplate>
													<TextBlock Text="{Binding Name}"/>
												</DataTemplate>
											</DataGridTemplateColumn.CellTemplate>
										</DataGridTemplateColumn>
										<!-- Quantity -->
										<DataGridTemplateColumn Header="{DynamicResource LoadoutConfigDetails.Items.Grid.Quantity}" Width="Auto">
											<DataGridTemplateColumn.CellTemplate>
												<DataTemplate>
													<TextBlock Text="{Binding Quantity}"/>
												</DataTemplate>
											</DataGridTemplateColumn.CellTemplate>
										</DataGridTemplateColumn>
										<!-- Actions -->
										<DataGridTemplateColumn Header="{DynamicResource LoadoutConfigDetails.Items.Grid.Actions}" Width="Auto">
											<DataGridTemplateColumn.CellTemplate>
												<DataTemplate>
													<controls:AraButton 
														Style="{StaticResource PageTitleButton}" 
														Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
														CommandParameter="{Binding}">
														<controls:AraButton.Children>
															<Image Style="{StaticResource ButtonImage}" Source="/Assets/Icons/DataGrid/Delete.png" />
														</controls:AraButton.Children>
													</controls:AraButton>
												</DataTemplate>
											</DataGridTemplateColumn.CellTemplate>
										</DataGridTemplateColumn>
									</DataGrid.Columns>
								</DataGrid>
							</Border>
							<!-- No Configurations Message -->
							<StackPanel Orientation="Vertical" >
								<StackPanel.Style>
									<Style TargetType="StackPanel">
										<Setter Property="Visibility" Value="Collapsed"/>
										<Style.Triggers>
											<DataTrigger Binding="{Binding SelectedItemsList.Count}" Value="0">
												<Setter Property="Visibility" Value="Visible" />
											</DataTrigger>
										</Style.Triggers>
									</Style>
								</StackPanel.Style>
								<TextBlock Text="{DynamicResource LoadoutConfigDetails.Items.Grid.EmptyTitle}">
									<TextBlock.Style>
										<Style TargetType="TextBlock" BasedOn="{StaticResource DataGridNoItemsMessage}">
											<Style.Triggers>
												<DataTrigger Binding="{Binding LoadoutValidation.IsItemsListNotValid}" Value="True">
													<Setter Property="Foreground" Value="{DynamicResource TextColorError}"/>
												</DataTrigger>
											</Style.Triggers>
										</Style>
									</TextBlock.Style>
								</TextBlock>
								<TextBlock Text="{DynamicResource LoadoutConfigDetails.Items.Grid.EmptySubTitle}">
									<TextBlock.Style>
										<Style TargetType="TextBlock" BasedOn="{StaticResource DataGridNoItemsMessage}">
											<Style.Triggers>
												<DataTrigger Binding="{Binding LoadoutValidation.IsItemsListNotValid}" Value="True">
													<Setter Property="Foreground" Value="{DynamicResource TextColorError}"/>
												</DataTrigger>
											</Style.Triggers>
										</Style>
									</TextBlock.Style>
								</TextBlock>
							</StackPanel>
						</Grid>
					</Grid>
				</GroupBox>
			</Grid>
		</ScrollViewer>

		<Grid Grid.Row="4">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>

			<!-- Validation Message -->
			<StackPanel Grid.Column="0" Orientation="Vertical" >
				<StackPanel.Style>
					<Style TargetType="StackPanel">
						<Setter Property="Visibility" Value="Collapsed"/>
						<Style.Triggers>
							<DataTrigger Binding="{Binding IsValid}" Value="False">
								<Setter Property="Visibility" Value="Visible" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</StackPanel.Style>
				<TextBlock Style="{StaticResource ValidationErrorMessage}" Text="{DynamicResource LoadoutConfigDetails.Items.Grid.ValidationTitle}"/>
				<TextBlock Style="{StaticResource ValidationErrorMessage}" Text="{DynamicResource LoadoutConfigDetails.Items.Grid.ValidationSubTitle}"/>
			</StackPanel>

			<controls:AraButton Grid.Column="1" Command="{Binding SaveLoadoutConfigurationCommand}">
				<controls:AraButton.Style>
					<Style TargetType="controls:AraButton" BasedOn="{StaticResource PrimaryButton}">
						<Style.Triggers>
							<DataTrigger Binding="{Binding IsEdited}" Value="False">
								<Setter Property="IsEnabled" Value="False" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</controls:AraButton.Style>
				<controls:AraButton.Children>
					<TextBlock Text="{DynamicResource General.Save}" />
				</controls:AraButton.Children>
			</controls:AraButton>
		</Grid>
	</Grid>
</UserControl>
