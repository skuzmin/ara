<UserControl x:Class="ARA.Views.LoadoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:pages="clr-namespace:ARA.ViewModels.Pages" 
			 xmlns:controls="clr-namespace:ARA.Controls.CustomControls"
			 xmlns:converters="clr-namespace:ARA.Converters"
			 xmlns:enums="clr-namespace:ARA.Enums"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 mc:Ignorable="d"
			 d:DataContext="{d:DesignInstance Type=pages:LoadoutViewModel}">
	<UserControl.Resources>
		<converters:StatusToImageConverter x:Key="StatusToImageConverter" />
	</UserControl.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="40" />
			<RowDefinition Height="12" />
			<RowDefinition Height="40" />
			<RowDefinition Height="20" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<Grid Grid.Row="0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="12" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>

			<TextBlock Grid.Column="0" Style="{StaticResource PageTitle}" Text="Loadout Checker" />
			<controls:AraButton Grid.Column="2" Style="{StaticResource PageTitleButton}" Command="{Binding Navigation.NavigateToPageCommand}" CommandParameter="{x:Static enums:AraPage.LoadoutConfigs}">
				<controls:AraButton.Children>
					<Image Style="{StaticResource ButtonImage}" Source="/Assets/Icons/config.png" />
				</controls:AraButton.Children>
			</controls:AraButton>
		</Grid>

		<Grid Grid.Row="2">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="12"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>
			<controls:AraComboBox
				x:Name="LoadoutComboBox"
				Style="{StaticResource AraComboBox}"
				ItemsSource="{Binding LoadoutOptions}"
				PlaceholderText="Select Loadout"
				DisplayMemberPath="Name"
				SelectedItem="{Binding SelectedLoadout, Mode=TwoWay}"/>
			<controls:AraButton Grid.Column="2" Command="{Binding CheckLoadout}">
				<controls:AraButton.Children>
					<Image Style="{StaticResource ButtonImage}" Source="/Assets/Icons/check-mark.png" />
					<TextBlock Text="Check" Margin="4 0 0 0"/>
				</controls:AraButton.Children>
				<controls:AraButton.Style>
					<Style TargetType="controls:AraButton" BasedOn="{StaticResource PrimaryButton}">
						<Style.Triggers>
							<DataTrigger Binding="{Binding SelectedLoadout}" Value="{x:Null}">
								<Setter Property="IsEnabled" Value="False" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</controls:AraButton.Style>
			</controls:AraButton>
		</Grid>
		<Grid Grid.Row="4">
			<!-- Loadout Items Grid -->
			<Border>
				<Border.Style>
					<Style TargetType="Border" BasedOn="{StaticResource DataGridBorderWrapper}">
						<Style.Triggers>
							<DataTrigger Binding="{Binding SelectedLoadout}" Value="{x:Null}">
								<Setter Property="Visibility" Value="Hidden"/>
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</Border.Style>
				<DataGrid 
					x:Name="LoadoutPreview"
					ItemsSource="{Binding SelectedLoadout.Items}">
					<DataGrid.Columns>
						<!-- Icon -->
						<DataGridTemplateColumn Header="Icon" Width="60">
							<DataGridTemplateColumn.CellTemplate>
								<DataTemplate>
									<Image Source="{Binding Path}" Height="48" Stretch="Uniform" VerticalAlignment="Center"/>
								</DataTemplate>
							</DataGridTemplateColumn.CellTemplate>
						</DataGridTemplateColumn>
						<!-- Name -->
						<DataGridTemplateColumn Header="Name" Width="*">
							<DataGridTemplateColumn.CellTemplate>
								<DataTemplate>
									<TextBlock Text="{Binding Name}"/>
								</DataTemplate>
							</DataGridTemplateColumn.CellTemplate>
						</DataGridTemplateColumn>
						<!-- Quantity -->
						<DataGridTemplateColumn Header="Quantity" Width="Auto">
							<DataGridTemplateColumn.CellTemplate>
								<DataTemplate>
									<TextBlock Text="{Binding Quantity}"/>
								</DataTemplate>
							</DataGridTemplateColumn.CellTemplate>
						</DataGridTemplateColumn>
						<!-- Status -->
						<DataGridTemplateColumn Header="Status" Width="Auto">
							<DataGridTemplateColumn.CellTemplate>
								<DataTemplate>
									<Image Source="{Binding Status, Converter={StaticResource StatusToImageConverter}}" Height="24" Width="24" Stretch="Uniform" />
								</DataTemplate>
							</DataGridTemplateColumn.CellTemplate>
						</DataGridTemplateColumn>
					</DataGrid.Columns>
				</DataGrid>
			</Border>
			<!-- No Loadouts Message -->
			<StackPanel Orientation="Vertical" >
				<StackPanel.Style>
					<Style TargetType="StackPanel">
						<Setter Property="Visibility" Value="Collapsed"/>
						<Style.Triggers>
							<DataTrigger Binding="{Binding ElementName=LoadoutComboBox, Path=Items.Count}" Value="0">
								<Setter Property="Visibility" Value="Visible" />
							</DataTrigger>
						</Style.Triggers>
					</Style>
				</StackPanel.Style>
				<TextBlock Style="{StaticResource DataGridNoItemsMessage}" Text="You don't have any Loadouts yet."/>
				<TextBlock Style="{StaticResource DataGridNoItemsMessage}" Text="Use the configuration button [ ⚙️ ] to create your first loadout."/>
			</StackPanel>
		</Grid>
	</Grid>
</UserControl>
